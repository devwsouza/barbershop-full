version:"3.8"
services:
 db:
  image: postgres:15
  environment:
   POSTGRES_DB: barbershop
   POSTGRES_USER: admin
   POSTGRES_PASSWORD: admin
  ports:["5432:5432"]
 backend:
  build: ./backend
  ports:["8080:8080"]
 frontend:
  build: ./frontend
  ports:["3000:3000"]
